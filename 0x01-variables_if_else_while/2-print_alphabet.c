#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{

char lwcs;

for (lwcs = 'a'; lwcs <= 'z'; lwcs++)
	putchar(lwcs);
	printf("\n");

return (0);
}
